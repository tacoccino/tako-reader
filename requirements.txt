# Tako Reader (タコReader)
# Install: pip install -r requirements.txt

PyQt6>=6.6.0
PyMuPDF>=1.23.0      # PDF support (imports as `fitz`)
Pillow>=10.0.0       # Image processing
numpy>=1.24.0        # Array ops for OCR image conversion
manga-ocr>=0.1.11    # Japanese OCR — downloads ~400 MB model on first use
                     # Comment out the line above if you don't need OCR
